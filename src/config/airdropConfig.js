// Airdrop program configuration
const airdropConfig = {
    // Set false to hide airdrop section, true to show
    IS_ACTIVE: true,
    
    // Minimum points required for eligibility
    MINIMUM_POINTS: 12000.00
};

export default airdropConfig;